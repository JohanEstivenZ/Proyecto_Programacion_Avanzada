spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=MariaDB
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.ai.openai.api-key=sk-test-dummy

# JWT ? necesario para que no falle el contexto
jwt.secret=clave-secreta-para-tests-taskflow-uniquindio-2026-larga
jwt.expiration=86400000

# Desactiva Spring AI en tests
spring.ai.openai.chat.enabled=false